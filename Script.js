// Add event listener to button
document.querySelector('button').addEventListener('click', () => {
    // Scroll to token info section
    document.querySelector('.token-info').scrollIntoView({ behavior: 'smooth' });
});
